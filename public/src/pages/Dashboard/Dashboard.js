import React from 'react';
import { Component } from 'react';
import {DashboardContainner} from '../../containers';

export default class Dashboard extends Component {
    render() {
        return (
            <DashboardContainner />
        );
    }
}